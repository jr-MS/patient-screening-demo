# 拷贝为 .env 并填入真实值
# 本项目为前端 Vite 应用：对浏览器可见的变量以 VITE_ 开头
# ===== Demo PDF（示例病历 PDF 链接）=====
# 建议：放一个可公开访问的Blob SAS URL
VITE_DEMO_PDF_URL=https://{your-blob-source}.core.windows.net/{your-pdf-file}?{your-sas-token}

# Vite dev server 代理 Blob 的目标域名（用于 /api/blob 反代，解决 CORS）
VITE_BLOB_PROXY_TARGET=https://{your-blob-source}.blob.core.windows.net

# ===== Azure Content Understanding =====
VITE_CU_ENDPOINT=https://{your-content-understanding-source}.cognitiveservices.azure.com
VITE_CU_SUBSCRIPTION_KEY={your-content-understanding-key}
VITE_CU_ANALYZER_ID={your-analyzer-id}
VITE_CU_API_VERSION=2025-11-01

# ===== Azure OpenAI（用于指标校验）=====
# 注意：前端直连会暴露 Key（仅建议 Demo/内网场景）。更安全做法是把调用放到后端。
VITE_AZURE_OPENAI_ENDPOINT=https://{your-azure-openai-source}.cognitiveservices.azure.com
VITE_AZURE_OPENAI_API_KEY={your-azure-openai-key}
VITE_AZURE_OPENAI_DEPLOYMENT={your-deployment-name}
VITE_AZURE_OPENAI_API_VERSION=2024-08-01-preview